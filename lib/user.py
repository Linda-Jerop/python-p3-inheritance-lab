#!/usr/bin/env python

class User:
    def __init__(self, first_name, last_name):  # initialize with first and last name
        self.first_name = first_name  # save first name as attribute
        self.last_name = last_name  # save last name as attribute